import 'package:flutter/material.dart';

class MethodOptionsDialog extends StatelessWidget {
  const MethodOptionsDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
